{% extends '@JasderoPassePlat/Admin/adminLayout.html.twig' %}

{% block passe_plat_content %}
    <div class="container">
        <div class="row">
            <h5 class="center-align">Status detail</h5>
            <table>
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ state.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ state.name }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ state.description }}</td>
                </tr>
                <tr>
                    <th>Color</th>
                    <td class="{{ state.color }}">{{ state.color }}</td>
                </tr>
                <tr>
                    <th>Activated</th>
                    <td>{% if state.activated %}Yes{% else %}No{% endif %}</td>
                </tr>
                </tbody>
            </table>

            <div class="iconsBlock col s12 center-align">
                <a href="{{ path('state_index') }}" title="back to the list">
                    <button type="button" class="btn-floating grey darken-3"><i class="material-icons">skip_previous</i></button>
                </a>
                <a href="{{ path('state_edit', { 'id': state.id }) }}" class="iconAsLink">
                    <button type="button" class="btn-floating grey darken-3" title="edit"><i class="material-icons">edit</i></button>
                </a>
                {{ form_start(delete_form) }}
                <button type="submit" title="delete" class="btn-floating grey darken-3"><i class="material-icons">delete_forever</i></button>
                {{ form_end(delete_form) }}
            </div>
        </div>
    </div>
{% endblock %}
