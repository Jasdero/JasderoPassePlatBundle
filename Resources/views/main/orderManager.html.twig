{% extends '@JasderoPassePlat/main/adminLayout.html.twig' %}
{% block passe_plat_content %}
    <div class="container">
        <div class="row">
            <div class="col s12">
                <h4 class="center-align">Manage {{ container }} in Google Drive</h4>
                <div class="center-align">
                    <a href="{{ path('checking', {'action' : true}) }}" class="btn">Scan folder for new {{ container }}</a>

                    {% if newOrders %}
                        <p><strong>Number of new {{ container }} created : {{ newOrders }}</strong></p>
                    {% endif %}
                    {% if errors %}
                        {% for error in errors %}
                            <p>{{ error }}</p><br>
                        {% endfor %}
                    {% endif %}
                    <h4>OR</h4>
                    <a href="{{ path('order_site_new') }}" class="btn light-green">Create a new {{ container }}</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
